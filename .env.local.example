# Payload CMS — core settings
PAYLOAD_SECRET=please-change-me-in-prod
DATABASE_URI=postgres://postgres:postgres@localhost:5432/authoring

# Public URL of this Payload instance (used for links and by the co-located frontend)
PAYLOAD_PUBLIC_URL=http://localhost:3000

# Frontend (Operator UI) talking to Payload running in the same project
# The frontend code prefers PAYLOAD_API_URL; if not set, it falls back to FRONTEND_PAYLOAD_API_URL.
PAYLOAD_API_URL=${PAYLOAD_PUBLIC_URL}
PAYLOAD_API_TOKEN=

FRONTEND_PAYLOAD_API_URL=${PAYLOAD_PUBLIC_URL}
FRONTEND_PAYLOAD_API_TOKEN=

# Optional: protect /api/preview with a shared secret (sent via X-Preview-Token)
FRONTEND_PREVIEW_TOKEN=

# If you enable FRONTEND_PREVIEW_TOKEN above, also expose the same value to the browser
# so the Operator UI can send X-Preview-Token automatically with its POST /api/preview call.
NEXT_PUBLIC_FRONTEND_PREVIEW_TOKEN=

# Expose values to the client (Next.js) when needed
NEXT_PUBLIC_PAYLOAD_API_URL=${PAYLOAD_PUBLIC_URL}
NEXT_PUBLIC_FRONTEND_PREVIEW_TOKEN=

# Optional: if you proxy or serve the frontend from a different host/port, override the *_API_URL values above.
