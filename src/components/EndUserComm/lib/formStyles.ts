// Shared form input text size
// Can be changed to text-sm, text-base, text-lg, etc.
export const FORM_INPUT_TEXT_SIZE = 'text-base';

// Centralized disabled styling constants
// Use these constants instead of hardcoding disabled styles

// For form inputs (input, textarea, select) - includes gray background
export const FORM_DISABLED = 'disabled:cursor-not-allowed disabled:opacity-50';

// For buttons and interactive elements - no background change needed
export const BUTTON_DISABLED = 'disabled:cursor-not-allowed disabled:opacity-50';
